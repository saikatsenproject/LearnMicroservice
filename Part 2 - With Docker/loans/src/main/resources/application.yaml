server:
  port: 9000
spring:
  application:
    name: loans
  datasource:
    url: jdbc:h2:mem:microServiceDB
    username: saikat
    password: saikat
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  h2:
    console:
      enabled: true
  config:
    import:
      - "application_dev.yaml"
      - "application_prod.yaml"
  profiles:
    active: dev